name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-publish:

    runs-on: ubuntu-latest

    steps:
      # 1. Checkout
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      
      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      # 2. Restore
      - name: Restore dependencies
        run: dotnet restore Serilog.TeachTeaStudio.Wrap.sln

      # 3. Build
      - name: Build
        run: dotnet build Serilog.TeachTeaStudio.Wrap.sln --no-restore --configuration Release

      # 4. Test (skip if no test projects exist)
      - name: Test
        run: dotnet test Serilog.TeachTeaStudio.Wrap.sln --no-build --verbosity normal || echo "No test projects found, skipping tests"
        continue-on-error: true

      # 5. Extract versions and pack
      - name: Extract versions and pack
        id: get-versions
        run: |
          mkdir -p artifacts
          for proj in LoggerLibrary/*.csproj; do
            if [[ ! -f "$proj" ]]; then
              echo "Skipping - project file not found: $proj"
              continue
            fi
            VERSION=$(grep -Po '(?<=<Version>)[^<]+' "$proj" || true)
            if [[ -z "$VERSION" ]]; then
              echo "Skipping $proj - no <Version> tag found"
              continue
            fi
            echo "Packing $proj version $VERSION"
            dotnet pack "$proj" \
              --configuration Release \
              --output ./artifacts \
              --no-build \
              /p:PackageVersion=$VERSION
          done

      # 7. Push all NuGet packages
      - name: Push all NuGet packages
        run: |
          for pkg in ./artifacts/*.nupkg; do
            echo "Pushing $pkg"
            dotnet nuget push "$pkg" \
              --source https://api.nuget.org/v3/index.json \
              --api-key ${{ secrets.NUGET_API_KEY }} \
              --skip-duplicate
          done

